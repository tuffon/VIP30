---
phase: 34.1-exact-category-preservation-no-umbrella-remapping-in-bid-comp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-python/vip_shared/pipeline/passes/trade_context.py
  - packages/shared-python/vip_shared/pipeline/models.py
  - packages/shared-python/tests/test_trade_context.py
autonomous: true
must_haves:
  truths:
    - "TradeContext category keys preserve exact parsed recap/trade labels instead of synthetic umbrella labels"
    - "Fake umbrella categories such as `Doors / Windows / Glass` and `Cabinetry / Millwork` no longer appear in pipeline category identity"
    - "trade_summary rows preserve raw trade/category identity rather than being remapped into umbrella buckets"
  artifacts:
    - path: "packages/shared-python/vip_shared/pipeline/passes/trade_context.py"
      provides: "Exact-category preservation in trade/recap category extraction"
      contains: "build_trade_context"
    - path: "packages/shared-python/tests/test_trade_context.py"
      provides: "Regression coverage for exact parsed category keys and absence of umbrella labels"
      contains: "Doors / Windows / Glass"
  key_links:
    - from: "trade_context.py"
      to: "test_trade_context.py"
      via: "exact category identity"
      pattern: "no umbrella remapping"
---

<objective>
Remove umbrella remapping from the pipeline category identity layer so `TradeContext` preserves the exact parsed category titles from `trade_summary` and `recap_by_category`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34.1-exact-category-preservation-no-umbrella-remapping-in-bid-comp/34.1-CONTEXT.md
@.planning/phases/34.1-exact-category-preservation-no-umbrella-remapping-in-bid-comp/34.1-RESEARCH.md

@packages/shared-python/vip_shared/pipeline/passes/trade_context.py
@packages/shared-python/vip_shared/pipeline/models.py
@packages/shared-python/tests/test_trade_context.py

Key facts:
- the parser already emits the real category labels
- fake umbrella labels are introduced after parsing by deterministic code
- Phase 34.1 requires exact parsed labels to remain intact through TradeContext
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing exact-category preservation tests</name>
  <files>packages/shared-python/tests/test_trade_context.py</files>
  <action>
Add regression tests that prove exact parsed labels are preserved in `TradeContext` and that fake umbrella labels such as `Doors / Windows / Glass` and `Cabinetry / Millwork` do not appear in pipeline category keys.
  </action>
  <verify>
    Tests fail before implementation if the current remap logic is still active.
  </verify>
  <done>
    Exact-label expectations are locked in tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove category umbrella remapping from trade/recap extraction</name>
  <files>packages/shared-python/vip_shared/pipeline/passes/trade_context.py, packages/shared-python/vip_shared/pipeline/models.py</files>
  <action>
Refactor the category identity path so trade-summary and recap rows preserve their exact parsed labels rather than flowing through the synthetic category remap layer.
  </action>
  <verify>
    `TradeContext` emits exact parsed category keys.
    Fake umbrella labels no longer appear in the category identity layer.
  </verify>
  <done>
    Pipeline category identity is faithful to the parsed source.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run targeted exact-category tests</name>
  <files>(verification only)</files>
  <action>
Run the updated trade-context tests to verify exact parsed category preservation.
  </action>
  <verify>
    Targeted tests pass.
  </verify>
  <done>
    Phase 34.1 foundation is verified.
  </done>
</task>

</tasks>

<verification>
- [ ] TradeContext preserves exact parsed category labels
- [ ] umbrella categories no longer appear in category identity
- [ ] targeted tests pass
</verification>
