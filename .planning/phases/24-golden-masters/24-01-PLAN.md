---
phase: 24-golden-masters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/parser/tests/golden/README.md
  - packages/parser/tests/golden/rough-drafts/lachman.golden.json
  - packages/parser/tests/golden/rough-drafts/kalyvas.golden.json
autonomous: true

must_haves:
  truths:
    - "packages/parser/tests/golden/ directory exists with README.md"
    - "lachman.golden.json exists and contains 32 sections with 525 line items (matches Phase 23 audit)"
    - "kalyvas.golden.json exists and contains 40 sections with 887 line items (matches Phase 23 audit)"
    - "Both rough-draft golden masters have all top-level fields populated: estimate_name, case_metadata, sections, grand_total_areas, coverage, recaps_and_summaries"
    - "Validation deltas are 0.00 on all sections in both rough-draft golden masters"
  artifacts:
    - path: "packages/parser/tests/golden/README.md"
      provides: "Golden master documentation — purpose, format, file naming, how to update"
      min_lines: 25
    - path: "packages/parser/tests/golden/rough-drafts/lachman.golden.json"
      provides: "Complete expected parser output for Lachman rough draft (32 sections, 525 items)"
      min_lines: 500
      contains: "\"estimate_name\""
    - path: "packages/parser/tests/golden/rough-drafts/kalyvas.golden.json"
      provides: "Complete expected parser output for Kalyvas rough draft (40 sections, 887 items)"
      min_lines: 500
      contains: "\"estimate_name\""
  key_links:
    - from: "packages/parser/tests/golden/rough-drafts/lachman.golden.json"
      to: "packages/parser/audit_output/rough-drafts/1115_LACHMAN_APEX_2_ROUGH_DRAFT_CAR.json"
      via: "golden master copied from verified parser output (zero validation delta confirmed in Phase 23 audit)"
    - from: "packages/parser/tests/golden/rough-drafts/kalyvas.golden.json"
      to: "packages/parser/audit_output/rough-drafts/KALYVAS_JVB_V6_KALY2_ROUGH_DRAFT_CAR.json"
      via: "golden master copied from verified parser output (zero validation delta confirmed in Phase 23 audit)"
---

<objective>
Create the golden master directory structure and produce complete golden master JSON files for both rough-draft document types.

Purpose: Rough-draft golden masters are the easiest of the 5 — the parser already produces production-quality output with zero validation delta across all sections. The golden master is the parser's own verified output, saved as ground truth. Phase 25 (coverage harness) will compare live parser runs against these golden masters.

Output:
- `packages/parser/tests/golden/README.md` — documentation for golden masters
- `packages/parser/tests/golden/rough-drafts/lachman.golden.json` — Lachman APEX rough draft ground truth (32 sections, 525 items)
- `packages/parser/tests/golden/rough-drafts/kalyvas.golden.json` — Kalyvas JVB rough draft ground truth (40 sections, 887 items)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/parser/audit_output/AUDIT-REPORT.md
@packages/parser/audit_output/run_log.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create golden master directory structure and README</name>
  <files>
    packages/parser/tests/golden/README.md
    packages/parser/tests/__init__.py (create if needed)
    packages/parser/tests/golden/rough-drafts/.gitkeep
    packages/parser/tests/golden/final-drafts/.gitkeep
    packages/parser/tests/golden/final-drafts/statefarm/.gitkeep
  </files>
  <action>
Create the directory tree at `packages/parser/tests/golden/` with subdirectories:
  - rough-drafts/
  - final-drafts/
  - final-drafts/statefarm/

Create `packages/parser/tests/golden/README.md` covering:
1. **Purpose**: Golden masters are the ground-truth expected output for each document type. Phase 25 harness diffs live parser output against these files to measure coverage.
2. **File naming convention**:
   - `rough-drafts/lachman.golden.json` — Lachman APEX 2 rough draft
   - `rough-drafts/kalyvas.golden.json` — Kalyvas JVB rough draft
   - `final-drafts/bschacter.golden.json` — BSchacter contractor final
   - `final-drafts/statefarm/customer_copy.golden.json` — StateFarm Customer Copy
   - `final-drafts/statefarm/lachman_sf.golden.json` — StateFarm Lachman structural
   - `final-drafts/statefarm/kalyvas_sf.golden.json` — StateFarm Kalyvas preliminary
3. **Schema**: Same as parser output schema (vip_parser.xactimate output). Top-level fields: estimate_name, case_metadata, sections, grand_total_areas, coverage, recaps_and_summaries, validations.
4. **How to update**: Run parser on source PDF, verify output quality, replace golden master file, human-verify against PDF, commit.
5. **Source PDFs**: Note where the source PDFs live (`docs/rough-drafts/`, `docs/final-drafts/`, `docs/final-drafts/statefarm/`).

Create `packages/parser/tests/__init__.py` if it doesn't exist (empty file to make tests a proper package).
  </action>
  <verify>ls packages/parser/tests/golden/ (shows rough-drafts/, final-drafts/, README.md)</verify>
  <done>Directory structure exists; README.md has all 6 sections (purpose, naming, schema, update process, source PDFs); at least 25 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Create rough-draft golden masters from verified parser output</name>
  <files>
    packages/parser/tests/golden/rough-drafts/lachman.golden.json
    packages/parser/tests/golden/rough-drafts/kalyvas.golden.json
  </files>
  <action>
The rough-draft parser outputs from Phase 23 are production-quality (zero validation delta, all fields populated). Copy them directly as golden masters.

Use Python to:
1. Read `packages/parser/audit_output/rough-drafts/1115_LACHMAN_APEX_2_ROUGH_DRAFT_CAR.json` (encoding='utf-8')
2. Verify it has: `sections` (should be 32), total line_items (should be 525), `grand_total_areas` populated, `coverage` populated, `recaps_and_summaries` populated.
3. Write to `packages/parser/tests/golden/rough-drafts/lachman.golden.json` (encoding='utf-8', indent=2)

4. Read `packages/parser/audit_output/rough-drafts/KALYVAS_JVB_V6_KALY2_ROUGH_DRAFT_CAR.json` (encoding='utf-8')
5. Verify: sections=40, total items=887.
6. Write to `packages/parser/tests/golden/rough-drafts/kalyvas.golden.json` (encoding='utf-8', indent=2)

Do NOT modify the JSON structure — copy verbatim. The parser output IS the golden master for rough drafts.

Verification script (run inline after creating files):
```python
import json
for path, exp_sec, exp_items in [
    ('packages/parser/tests/golden/rough-drafts/lachman.golden.json', 32, 525),
    ('packages/parser/tests/golden/rough-drafts/kalyvas.golden.json', 40, 887),
]:
    with open(path, encoding='utf-8') as f:
        d = json.load(f)
    assert len(d['sections']) == exp_sec, f"Expected {exp_sec} sections"
    actual_items = sum(len(s.get('line_items', [])) for s in d['sections'])
    assert actual_items == exp_items, f"Expected {exp_items} items"
    assert d.get('grand_total_areas'), "grand_total_areas missing"
    assert d.get('coverage'), "coverage missing"
    print(f"{path}: OK (sections={exp_sec}, items={exp_items})")
```
  </action>
  <verify>
python -c "
import json
for path, exp_sec, exp_items in [('packages/parser/tests/golden/rough-drafts/lachman.golden.json', 32, 525), ('packages/parser/tests/golden/rough-drafts/kalyvas.golden.json', 40, 887)]:
    with open(path, encoding='utf-8') as f: d = json.load(f)
    assert len(d['sections']) == exp_sec
    assert sum(len(s.get('line_items', [])) for s in d['sections']) == exp_items
    print(f'{path}: sections={exp_sec}, items={exp_items} OK')
"
  </verify>
  <done>Both golden master JSON files exist; section counts and item counts match Phase 23 audit; all top-level fields populated; files are valid JSON.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `packages/parser/tests/golden/` directory exists with 3 subdirectories
- [ ] `README.md` exists with ≥25 lines covering all 6 documentation sections
- [ ] `lachman.golden.json` valid JSON, 32 sections, 525 items
- [ ] `kalyvas.golden.json` valid JSON, 40 sections, 887 items
- [ ] Both golden masters have `estimate_name`, `case_metadata`, `sections`, `grand_total_areas`, `coverage`, `recaps_and_summaries`
</verification>

<success_criteria>
- Directory structure created at packages/parser/tests/golden/
- README.md documents golden master purpose, naming, schema, update process
- Both rough-draft golden masters copied from Phase 23 verified parser output
- Section and item counts verified programmatically (32/525, 40/887)
- All files committed
</success_criteria>

<output>
After completion, create `.planning/phases/24-golden-masters/24-01-SUMMARY.md`
</output>
