---
phase: 12-output-modes-enhanced-xlsx
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - apps/vip-parse/src/db/models.py
  - apps/vip-parse/src/routes/jobs.py
  - apps/vip-parse/src/services/jobs.py
  - apps/vip-parse/src/tasks.py
  - apps/vip-parse/src/bid_comp/core.py
  - apps/vipclaims-saas/app/bid-comp/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can select output mode (Executive/Carrier/Litigation/Internal) before job submission"
    - "Selected output_mode persists in ComparisonJob database record"
    - "output_mode flows from API → worker → BidComp → export_xlsx"
    - "Default output_mode is 'internal' when not specified (backward compatible)"
    - "Audit trail in XLSX includes input file SHA-256 hashes"
    - "Frontend mode selector shows 4 options with descriptions"
  artifacts:
    - path: "apps/vip-parse/src/db/models.py"
      provides: "ComparisonJob with output_mode column"
      contains: "output_mode"
    - path: "apps/vip-parse/src/routes/jobs.py"
      provides: "API accepting output_mode parameter"
      contains: "output_mode"
    - path: "apps/vip-parse/src/tasks.py"
      provides: "Worker passing output_mode through pipeline"
      contains: "output_mode"
    - path: "apps/vip-parse/src/bid_comp/core.py"
      provides: "BidComp.run() accepting and forwarding output_mode"
      contains: "output_mode"
    - path: "apps/vipclaims-saas/app/bid-comp/page.tsx"
      provides: "Frontend mode selector UI"
      contains: "output_mode"
  key_links:
    - from: "apps/vipclaims-saas/app/bid-comp/page.tsx"
      to: "apps/vip-parse/src/routes/jobs.py"
      via: "POST /jobs with output_mode in request body"
      pattern: "output_mode"
    - from: "apps/vip-parse/src/routes/jobs.py"
      to: "apps/vip-parse/src/tasks.py"
      via: "RQ enqueue passes output_mode to worker"
      pattern: "output_mode"
    - from: "apps/vip-parse/src/tasks.py"
      to: "apps/vip-parse/src/bid_comp/core.py"
      via: "BidComp.run() receives output_mode"
      pattern: "output_mode"
    - from: "apps/vip-parse/src/bid_comp/core.py"
      to: "apps/vip-parse/src/bid_comp/export_xlsx.py"
      via: "export_xlsx() receives output_mode parameter"
      pattern: "output_mode"
---

<objective>
Wire output_mode through the full stack: database column, API endpoint, RQ worker, BidComp core, and frontend mode selector. Add file hash computation for audit trail.

Purpose: Complete the end-to-end flow so users can select an output mode before submission and receive mode-appropriate XLSX output.
Output: Modified DB model, API, worker, core, and frontend files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-output-modes-enhanced-xlsx/12-01-SUMMARY.md
@apps/vip-parse/src/db/models.py
@apps/vip-parse/src/routes/jobs.py
@apps/vip-parse/src/services/jobs.py
@apps/vip-parse/src/tasks.py
@apps/vip-parse/src/bid_comp/core.py
@apps/vipclaims-saas/app/bid-comp/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add output_mode to database model, API, and job service</name>
  <files>apps/vip-parse/src/db/models.py, apps/vip-parse/src/routes/jobs.py, apps/vip-parse/src/services/jobs.py</files>
  <action>
**db/models.py:**
Add `output_mode: Optional[str] = Field(default="internal", max_length=20)` to ComparisonJob model. Use string type (not enum) in the DB layer for simplicity — validation happens at the API layer.

**routes/jobs.py:**
1. Add `output_mode: Optional[str] = "internal"` to CreateJobRequest with Pydantic validator that restricts to ["executive", "carrier", "litigation", "internal"].
2. Pass output_mode to JobService.create_job().
3. Add output_mode to the RQ enqueue args for run_bid_comp_keys(). Add it as a keyword argument AFTER the existing positional args to maintain backward compatibility: `_q.enqueue(run_bid_comp_keys, ..., output_mode=payload.output_mode, ...)`.
4. Update retry_job to pass original_job.output_mode through to the new job and worker.
5. Add output_mode to JobStatusResponse model and _serialize_job().

**services/jobs.py:**
Add `output_mode: str = "internal"` parameter to JobService.create_job() and pass it to the ComparisonJob constructor.

Do NOT create an Alembic migration — this project uses SQLModel and the user will handle migrations separately. Just update the model.
  </action>
  <verify>python -c "from src.db.models import ComparisonJob; assert hasattr(ComparisonJob, 'output_mode'); print('OK')" from apps/vip-parse/</verify>
  <done>ComparisonJob has output_mode field, CreateJobRequest accepts and validates output_mode, RQ enqueue passes output_mode to worker</done>
</task>

<task type="auto">
  <name>Task 2: Thread output_mode through worker and BidComp core with file hash audit</name>
  <files>apps/vip-parse/src/tasks.py, apps/vip-parse/src/bid_comp/core.py</files>
  <action>
**tasks.py:**
1. Add `output_mode: str = "internal"` parameter to `run_bid_comp_keys()` function signature. Place it after `db_job_id` as a keyword-only argument.
2. After downloading and parsing PDFs, compute SHA-256 hashes of both input files: `import hashlib; carrier_hash = hashlib.sha256(carrier_path.read_bytes()).hexdigest(); contractor_hash = hashlib.sha256(contractor_path.read_bytes()).hexdigest()`.
3. Build `audit_metadata` dict: `{"primary_hash": carrier_hash, "comparison_hash": contractor_hash, "primary_filename": carrier_original, "comparison_filename": contractor_original, "output_mode": output_mode, "analysis_timestamp": datetime.utcnow().isoformat()}`.
4. Pass `output_mode` and `audit_metadata` to `comp.run()`.

**bid_comp/core.py:**
1. Add `output_mode: str = "internal"` and `audit_metadata: Optional[Dict] = None` parameters to `BidComp.run()`.
2. Import OutputMode from pipeline.output_modes.
3. Convert string to enum: `mode = OutputMode(output_mode)`.
4. Pass `output_mode=mode` and `audit_metadata=audit_metadata` to `export_xlsx()`.
5. Also pass `methodology=methodology_result` to `export_xlsx()` (the MethodologyResult from the analyzer, already available in BidComp.run's scope).

Keep all existing BidComp.run() logic intact — just add the new parameters and thread them to export_xlsx().
  </action>
  <verify>python -c "from src.tasks import run_bid_comp_keys; import inspect; sig = inspect.signature(run_bid_comp_keys); assert 'output_mode' in sig.parameters; print('OK')" from apps/vip-parse/</verify>
  <done>output_mode flows from worker → BidComp → export_xlsx, file hashes computed and included in audit_metadata</done>
</task>

<task type="auto">
  <name>Task 3: Add output mode selector to frontend</name>
  <files>apps/vipclaims-saas/app/bid-comp/page.tsx</files>
  <action>
Add an output mode selector to the bid comparison submission form. This is a Next.js 14 app with Tailwind CSS.

1. Add state: `const [outputMode, setOutputMode] = useState<string>("internal")`.
2. Add a mode selector UI ABOVE the submit button. Use a simple radio group or segmented control with 4 options:
   - "Internal" (default) — "Full detail, all sections"
   - "Executive" — "1-page summary: total delta, top 3 drivers"
   - "Carrier" — "Negotiation-ready with methodology detail"
   - "Litigation" — "Neutral tone, full evidence citations"
3. Style with Tailwind: grid of 4 cards/buttons, selected state with ring/border highlight. Keep it simple — no new components or libraries needed.
4. Include `output_mode: outputMode` in the POST /jobs request body alongside the existing carrier_key, contractor_key, etc.

Look at the existing page.tsx patterns for styling conventions (button styles, color scheme, spacing). Match the existing design language. The selector should be clearly labeled "Output Mode" with a brief description for each option.

Do NOT add a separate component file — keep it inline in page.tsx since it's a simple radio group.
  </action>
  <verify>Visually inspect that page.tsx contains output_mode state, mode selector UI, and includes output_mode in the POST body</verify>
  <done>Frontend shows 4 output mode options, default is "internal", selection included in job creation request</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ComparisonJob model has output_mode field
- [ ] CreateJobRequest validates output_mode to 4 allowed values
- [ ] run_bid_comp_keys accepts output_mode parameter
- [ ] BidComp.run() passes output_mode to export_xlsx()
- [ ] File SHA-256 hashes computed and included in audit_metadata
- [ ] Frontend page.tsx has mode selector with 4 options
- [ ] POST /jobs request includes output_mode
- [ ] Default output_mode is "internal" at every layer (backward compatible)
</verification>

<success_criteria>

- output_mode persists in database on ComparisonJob
- Full pipeline flow: frontend → API → worker → BidComp → export_xlsx with output_mode
- File hashes computed for audit trail
- Frontend mode selector with 4 options and clear descriptions
- Backward compatible — omitting output_mode defaults to "internal" everywhere
- No breaking changes to existing API consumers
</success_criteria>

<output>
After completion, create `.planning/phases/12-output-modes-enhanced-xlsx/12-02-SUMMARY.md`
</output>
