---
phase: 07-app-logged-in-experience
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/vipclaims-saas/app/bid-comp/page.tsx
  - apps/vipclaims-saas/components/CreditBalance.tsx
autonomous: false
must_haves:
  truths:
    - "Bid Comp page displays credit balance fetched from backend"
    - "Balance shows visual warning states (low, zero)"
    - "User sees clear feedback when credits are insufficient"
  artifacts:
    - path: "apps/vipclaims-saas/app/bid-comp/page.tsx"
      provides: "Credit balance display in upload UI"
      contains: "credits/balance"
  key_links:
    - from: "bid-comp/page.tsx"
      to: "/credits/balance"
      via: "Credit balance fetch"
      pattern: "credits/balance"
---

<objective>
Enhance credit balance display in Bid Comp UI with proper visual states.

Purpose: Users need clear visibility into their credit balance before submitting jobs, with appropriate warnings for low/zero balance states.
Output: Enhanced credit display in Bid Comp page with visual indicators.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-app-logged-in-experience/07-01-SUMMARY.md
@apps/vipclaims-saas/app/bid-comp/page.tsx
@apps/vipclaims-saas/components/CreditBalance.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance credit balance display in Bid Comp page</name>
  <files>apps/vipclaims-saas/app/bid-comp/page.tsx</files>
  <action>
    Update credit display section (currently around line 251-254):

    Current state:
    - Simple text: "Available credits: {balance}"
    - Fetches from /auth/me endpoint (expects credit_balance field)

    Changes needed:
    1. Fetch from /credits/balance endpoint directly for accurate balance
       - This endpoint returns { balance: number }
       - Already used by CreditBalance component

    2. Create visual credit card/badge:
       - Container: rounded-xl p-4 border
       - States:
         - balance > 2: bg-emerald-50 border-emerald-200 text-emerald-700
         - balance 1-2: bg-amber-50 border-amber-200 text-amber-700 + "Low balance" indicator
         - balance 0: bg-rose-50 border-rose-200 text-rose-700 + "No credits" warning

    3. Content structure:
       - Left: "Credits" label (text-sm)
       - Center: Balance number (text-2xl font-bold)
       - Right (if low): Warning icon or "Low" badge

    4. Message below card:
       - balance > 0: "Each comparison uses 1 credit"
       - balance = 0: "You need credits to generate comparisons. Contact us to add more."

    5. Disable submit button when balance = 0:
       - Add disabled state with opacity-50 cursor-not-allowed
       - Already partially implemented via canSubmit logic
  </action>
  <verify>npm run dev shows credit card with correct visual states based on balance</verify>
  <done>Credit display shows balance with visual states (good/low/zero)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Enhanced credit balance display in Bid Comp UI</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Log in to your account
    3. Visit: http://localhost:3000/bid-comp
    4. Check credit display section:
       - Verify balance number is accurate (matches header CreditBalance)
       - Verify visual styling matches balance state:
         - Green tones if balance > 2
         - Amber/warning if balance 1-2
         - Red/error if balance 0
    5. If balance = 0:
       - Verify submit button is disabled
       - Verify warning message is displayed
    6. If balance > 0:
       - Verify "Each comparison uses 1 credit" message
       - Verify submit button is enabled
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Credit balance fetched from /credits/balance endpoint
- [ ] Visual states correctly reflect balance level
- [ ] Zero balance disables submit button with clear messaging
- [ ] Low balance shows warning indicator
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- User approved credit display behavior
- Clear feedback prevents confusion about credit state
</success_criteria>

<output>
After completion, create `.planning/phases/07-app-logged-in-experience/07-02-SUMMARY.md`
</output>
