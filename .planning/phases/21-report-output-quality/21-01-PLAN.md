---
phase: 21-report-output-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/prompts/writer_pass_v1.json
  - apps/worker/src/prompts/writer_pass_v1.json
autonomous: true

must_haves:
  truths:
    - "Prompt version is 2.3 in both api and worker copies"
    - "Overview JSON schema specifies 4-6 sentences (not 2-3)"
    - "CRITICAL RULES in user prompt give sentence-by-sentence guidance for overview"
    - "System prompt has SUGGESTED FOLLOWUPS RULES with BAD/GOOD examples"
    - "Suggested follow-ups reference specific categories and dollar amounts"
  artifacts:
    - path: "apps/api/src/prompts/writer_pass_v1.json"
      provides: "v2.3 writer prompt"
      contains: "\"version\": \"2.3\""
    - path: "apps/worker/src/prompts/writer_pass_v1.json"
      provides: "v2.3 writer prompt (worker copy)"
      contains: "\"version\": \"2.3\""
  key_links:
    - from: "user prompt overview schema"
      to: "system prompt overview rules"
      via: "both say 4-6 sentences"
      pattern: "4-6 sentences"
    - from: "system prompt SUGGESTED FOLLOWUPS RULES"
      to: "LLM output suggested_followups"
      via: "BAD/GOOD examples guide output"
      pattern: "SUGGESTED FOLLOWUPS"
---

<objective>
Update writer_pass_v1 prompt from v2.2 to v2.3 to fix two quality gaps: (1) the overview JSON schema contradicts the system prompt (says "2-3 sentences" while system says "4-6"), causing the LLM to write short overviews; (2) the suggested_followups have no guidance, causing the LLM to produce generic "review line items" advice.

Purpose: The overview is the first thing an adjuster reads — it must be substantive and specific. Follow-ups must be actionable and reference actual trade categories, not generic advice.

Output: Both writer_pass_v1.json files updated to v2.3.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/api/src/prompts/writer_pass_v1.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SUGGESTED FOLLOWUPS RULES to system prompt</name>
  <files>apps/api/src/prompts/writer_pass_v1.json</files>
  <action>
In the `system` string, find the CRITICAL - NEUTRAL TONE section which reads:

```
CRITICAL - NEUTRAL TONE:
- DO NOT judge which estimate is "correct" or "necessary"
- DO NOT say "full extent of necessary work" or "underestimation"
- DO say "Primary scopes X while comparison scopes Y" - neutral observation
- The reader decides which approach is appropriate for the claim
```

Insert a new SUGGESTED FOLLOWUPS RULES section BEFORE the CRITICAL - NEUTRAL TONE section:

```
SUGGESTED FOLLOWUPS RULES:
- Every follow-up MUST name a specific category, dollar amount, or estimate
- Focus on the highest-delta categories where clarification changes the analysis
- Follow-ups should help the adjuster close gaps, not restate what the numbers already show

BAD follow-ups (generic, zero value - NEVER write these):
- "Request detailed breakdown of scope of work"
- "Review line items for accuracy"
- "Clarify methodology differences between estimates"
- "Obtain additional documentation to support findings"

GOOD follow-ups (specific, actionable, name actual trades and amounts):
- "Request itemized breakdown of Apex's $98,270 HVAC scope — confirm whether full system replacement or component-level repair"
- "Ask Carrier why LANDSCAPING ($72,847 in Apex) is excluded — determine if out-of-scope or estimated separately"
- "Request Apex to confirm O&P basis — if applied to subcontractor trades, explain why comparison excludes"
- "Obtain Carrier's window schedule — Apex scopes 14 impact-rated units at $1,800 each; clarify whether Carrier contemplates the same count"
- "Ask both parties to confirm square footage basis for PAINTING — Apex at 4,200 SF vs Carrier at 2,800 SF"

Use actual estimate names and dollar amounts from the data you are given.
```
  </action>
  <verify>
  grep "SUGGESTED FOLLOWUPS RULES" apps/api/src/prompts/writer_pass_v1.json
  grep "BAD follow-ups" apps/api/src/prompts/writer_pass_v1.json
  </verify>
  <done>System prompt contains SUGGESTED FOLLOWUPS RULES section with BAD/GOOD examples using specific categories and amounts.</done>
</task>

<task type="auto">
  <name>Task 2: Fix overview schema, tighten CRITICAL RULES, bump version, sync worker</name>
  <files>apps/api/src/prompts/writer_pass_v1.json, apps/worker/src/prompts/writer_pass_v1.json</files>
  <action>
**Change 1 — Fix overview JSON schema in user prompt.**
Find: `"overview": "2-3 sentences explaining the delta"`
Replace with: `"overview": "Full paragraph (4-6 sentences): (1) loss type + affected areas + approach difference, (2) top 2-3 trade categories with dollar amounts where this manifests, (3) specific items/quantities per driver category, (4) methodology differences (O&P, material grades)"`

**Change 2 — Tighten overview CRITICAL RULES in user prompt.**
Find:
```
- overview sentences 2-6: Then cover categories, specific items, methodology differences
```
Replace with:
```
- overview sentence 2: NAME the top 2-3 trade categories WITH DOLLAR AMOUNTS where the approach difference shows ($X vs $Y)
- overview sentences 3-4: For each top driver, state WHAT SPECIFIC ITEMS differ (e.g., "Apex includes 14 impact windows at $1,800 each; Carrier scopes reglazing")
- overview sentences 5-6: Note methodology differences — O&P treatment, material grades, emergency vs standard pricing
```

**Change 3 — Bump version.**
Find: `"version": "2.2"`
Replace with: `"version": "2.3"`

**After all edits to apps/api/src/prompts/writer_pass_v1.json, copy verbatim to apps/worker/src/prompts/writer_pass_v1.json.**
  </action>
  <verify>
  python -c "
import json
d = json.load(open('apps/api/src/prompts/writer_pass_v1.json'))
assert d['metadata']['version'] == '2.3', f'version={d[\"metadata\"][\"version\"]}'
assert 'Full paragraph' in d['user'], 'overview schema not updated'
assert 'sentence 2: NAME the top' in d['user'], 'CRITICAL RULES not updated'
assert 'SUGGESTED FOLLOWUPS RULES' in d['system'], 'system missing followups rules'
print('api v2.3 OK')
"
  python -c "import json; d=json.load(open('apps/worker/src/prompts/writer_pass_v1.json')); assert d['metadata']['version']=='2.3'; print('worker OK')"
  diff apps/api/src/prompts/writer_pass_v1.json apps/worker/src/prompts/writer_pass_v1.json && echo "Files identical"
  </verify>
  <done>Both files at v2.3. Overview schema specifies 4-6 sentences. CRITICAL RULES give sentence-by-sentence overview structure. Files are identical. Valid JSON.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m json.tool apps/api/src/prompts/writer_pass_v1.json > /dev/null` — valid JSON
- [ ] `grep '"version"' apps/api/src/prompts/writer_pass_v1.json` shows `"2.3"`
- [ ] `grep "SUGGESTED FOLLOWUPS" apps/api/src/prompts/writer_pass_v1.json` returns match
- [ ] `grep "Full paragraph" apps/api/src/prompts/writer_pass_v1.json` returns match
- [ ] `grep "sentence 2: NAME" apps/api/src/prompts/writer_pass_v1.json` returns match
- [ ] `diff apps/api/src/prompts/writer_pass_v1.json apps/worker/src/prompts/writer_pass_v1.json` shows no differences
- [ ] Run writer tests: `cd apps/api && PYTHONPATH=/mnt/c/Users/benav.TUFFON_AMD/Documents/Projects/VIP30/packages/shared-python python -m pytest tests/test_writer_pass.py -v`
</verification>

<success_criteria>
- Both prompt files at v2.3
- Overview schema corrected to "Full paragraph (4-6 sentences)" with sentence-by-sentence guidance
- SUGGESTED FOLLOWUPS RULES added with clear BAD/GOOD examples
- Files are identical and valid JSON
- Existing writer pass tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-report-output-quality/21-01-SUMMARY.md`
</output>
