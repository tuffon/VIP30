---
phase: 20-cost-driver-narrative-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/prompts/writer_pass_v1.json
  - apps/worker/src/prompts/writer_pass_v1.json
autonomous: true

must_haves:
  truths:
    - "Prompt version is 2.2 in both api and worker copies"
    - "Overview sentence 1 instruction opens with loss type AND approach difference, not abstract dimensions"
    - "APPROACH EXAMPLES table (7 pairs) present in system prompt"
    - "DIFFERENCE DIMENSIONS section and 'Do NOT default to restoration vs repair' instruction removed"
    - "Narrative placeholder in user prompt says THREE sentences, not two"
  artifacts:
    - path: "apps/api/src/prompts/writer_pass_v1.json"
      provides: "v2.2 writer prompt for api service"
      contains: "\"version\": \"2.2\""
    - path: "apps/worker/src/prompts/writer_pass_v1.json"
      provides: "v2.2 writer prompt for worker service"
      contains: "\"version\": \"2.2\""
  key_links:
    - from: "system prompt OVERVIEW RULES"
      to: "APPROACH EXAMPLES table"
      via: "Sentence 1 instruction references approach pairs"
      pattern: "APPROACH EXAMPLES"
    - from: "user prompt narrative schema"
      to: "system prompt style rules"
      via: "both say THREE sentences"
      pattern: "THREE sentences"
---

<objective>
Update writer_pass_v1 prompt from v2.1 to v2.2 by restoring the v1.9 concrete approach-examples table and merging it with v2.1's loss type context. Fix the "Two sentences" vs "THREE sentences" contradiction in the user prompt JSON schema.

Purpose: v2.0 replaced the concrete 7-pair approach table with an abstract 5-dimension framework, forcing the LLM to reason about which dimension applies before writing — weakening overviews. v2.2 restores the vocabulary-based approach table so the LLM can pattern-match directly to a pair and write confidently.

Output: Updated writer_pass_v1.json in both apps/api and apps/worker with version bumped to 2.2.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/api/src/prompts/writer_pass_v1.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update system prompt — restore approach table, remove abstract dimensions</name>
  <files>apps/api/src/prompts/writer_pass_v1.json</files>
  <action>
Make targeted edits to the `system` string in the JSON. The file is a single JSON object — edit in-place.

**Change 1 — OVERVIEW RULES sentence count:**
Find: `OVERVIEW RULES (write a full paragraph, 5-7 sentences):`
Replace with: `OVERVIEW RULES (write a full paragraph, 4-6 sentences):`

**Change 2 — Remove the two placeholder lines after Example openers block and replace full section:**
Find this entire block (from the example openers ending through the DIFFERENCE DIMENSIONS section ending at "material grades"):

```
- Sentence 1: Open with the loss type and affected areas, then state the key difference between estimates
- Sentence 2-3: Name the top 2-3 trade categories where this difference manifests

DIFFERENCE DIMENSIONS - Estimates can differ along many axes. Identify the TRUE difference:

1. SCOPE BREADTH: How many areas/rooms/systems are included?
   - "covers entire first floor" vs "addresses kitchen and bathroom only"
   - "includes all four bathrooms" vs "scopes master bath only"

2. SCOPE DEPTH: How thoroughly is each area addressed?
   - "full tear-out to studs" vs "surface repairs only"
   - "replaces all 14 windows" vs "replaces 6 damaged units"

3. MATERIAL GRADE: What quality level of materials?
   - "specifies Pella Architect Series windows" vs "standard builder-grade vinyl"
   - "luxury vinyl plank at $8/SF" vs "sheet vinyl at $3/SF"
   - "custom cabinetry" vs "stock cabinets"

4. METHODOLOGY: How is the work approached?
   - "tear-off and replace" vs "overlay existing"
   - "repair in place" vs "remove and reinstall"
   - "emergency/expedited rates" vs "standard pricing"

5. COMPLETENESS: What ancillary work is included?
   - "includes O&P, permits, and dumpster" vs "labor and materials only"
   - "full MEP coordination" vs "finish trades only"

IMPORTANT: Do NOT default to "restoration vs repair" framing. Both estimates may take a restoration approach but differ in material quality, scope depth, or completeness. Describe what you ACTUALLY observe in the data.

Example first sentences (loss type + nuanced differences):
- "Both estimates address the water damage affecting the first-floor kitchen and living room, but Apex specifies premium materials (quartz counters, custom cabinetry) while Carrier uses builder-grade finishes throughout."
- "For this water intrusion loss, the Lachman estimate addresses all four bathrooms while SF_Damage scopes only the two directly affected."
- "Following the storm damage to the roof and interior ceilings, both estimates take a replacement approach for flooring, but the contractor bid covers 2,400 SF including hallways while the carrier estimate limits scope to the 1,800 SF in damaged rooms."
- "For this emergency water loss, the carrier's estimate reflects after-hours response pricing ($185/hr) while the contractor's bid uses standard rates ($95/hr) for the same PWI scope."
- "Both estimates address fire damage to the master suite, but Apex includes tear-out to studs with full drywall replacement while Carrier scopes patching and skim-coating the existing surfaces."
```

Replace with:

```
- Sentence 1 (MOST IMPORTANT): Open with the loss type and affected areas, then state the fundamental APPROACH difference between estimates.

APPROACH EXAMPLES - Use these as inspiration, but craft language that best fits what you observe:

| Example Primary Approach | vs | Example Comparison Approach |
|--------------------------|----|-----------------------------|
| Full restoration | vs | Targeted repair |
| Replacement-in-kind | vs | Repair-in-place |
| Code upgrade | vs | As-was restoration |
| Comprehensive rebuild | vs | Cosmetic/surface repairs |
| Multi-trade coordination | vs | Single-trade scope |
| Premium materials | vs | Standard/builder grade |
| Emergency/expedited | vs | Standard timeline |

These are EXAMPLES, not a closed list. Use language that accurately describes the specific approach difference you observe. If the data suggests a different characterization (e.g., "tear-out and replace" vs "patch and repair", or "full system replacement" vs "component-level fixes"), use that instead.

Example first sentences (loss type + approach difference):
- "Both estimates address water damage to the first-floor kitchen and adjacent living areas, but Apex scopes full restoration while Carrier targets affected areas only."
- "For this fire loss affecting the master bedroom suite, the Apex estimate takes a tear-out and rebuild approach while Carrier contemplates surface repairs and skim-coating."
- "Following the wind event that damaged the roof and south-facing windows, Lachman_Structural scopes replacement-in-kind while SF_Damage_Assessment allows repair-in-place."
- "The Apex estimate takes a full restoration approach while the Carrier estimate scopes targeted repairs to affected areas only."
- "Lachman_Structural contemplates tear-out and replacement while SF_Damage_Assessment scopes repair of existing materials."
- "Both estimates address fire damage to the master suite, but Apex includes tear-out to studs with full drywall replacement while Carrier scopes patching and skim-coating the existing surfaces."
```

The lines after (Sentence 2, Sentence 3-4, Sentence 5-6) are already in place below this block in the current file — leave them untouched.
  </action>
  <verify>
  Grep the file for "DIFFERENCE DIMENSIONS" — must return no results.
  Grep the file for "APPROACH EXAMPLES" — must return a result.
  Grep the file for "Do NOT default" — must return no results.
  </verify>
  <done>System prompt contains APPROACH EXAMPLES table, DIFFERENCE DIMENSIONS section is gone, "Do NOT default" instruction is gone, sentence 1 instruction opens with loss type AND approach difference.</done>
</task>

<task type="auto">
  <name>Task 2: Fix user prompt schema + metadata, copy to worker</name>
  <files>apps/api/src/prompts/writer_pass_v1.json, apps/worker/src/prompts/writer_pass_v1.json</files>
  <action>
**In apps/api/src/prompts/writer_pass_v1.json:**

**Change 3 — Fix narrative placeholder in user prompt:**
Find: `"narrative": "Two sentences explaining what specific items drive this delta"`
Replace with: `"narrative": "THREE sentences: (1) what one estimate includes that the other doesn't, (2) specific quantities/materials/scope, (3) what the other estimate scopes instead"`

**Change 4 — Fix sentence 1 critical rule in user prompt:**
Find: `overview sentence 1: OPEN WITH LOSS TYPE AND AFFECTED AREAS, then state the key difference (scope breadth, depth, material grade, methodology, or completeness)`
Replace with: `overview sentence 1: OPEN WITH LOSS TYPE AND AFFECTED AREAS, then state the fundamental APPROACH DIFFERENCE (restoration vs repair, replacement vs repair-in-place, etc.)`

**Change 5 — Bump version in metadata:**
Find: `"version": "2.1"`
Replace with: `"version": "2.2"`

**Change 6 — Update metadata overview_structure:**
Find: `"style": "key-difference-first, neutral observation"`
Replace with: `"style": "approach-first, neutral observation"`

Find the entire `"difference_dimensions"` object in metadata:
```json
"difference_dimensions": {
  "note": "LLM must identify the TRUE difference - do not default to restoration vs repair",
  "dimensions": [
    "scope_breadth: how many areas/rooms/systems included",
    "scope_depth: how thoroughly each area addressed",
    "material_grade: quality level of specified materials",
    "methodology: tear-out vs overlay, repair vs replace",
    "completeness: O&P, permits, coordination included"
  ]
},
```
Replace with:
```json
"approach_examples": {
  "note": "Examples only - LLM should craft language that fits the specific data",
  "pairs": [
    "full restoration vs targeted repair",
    "replacement-in-kind vs repair-in-place",
    "code upgrade vs as-was restoration",
    "comprehensive rebuild vs cosmetic repairs",
    "multi-trade coordination vs single-trade scope",
    "premium materials vs standard grade",
    "emergency/expedited vs standard timeline"
  ]
},
```

Also update `"content"` in overview_structure:
Find: `"FIRST: loss type and affected areas, then key difference along one of the 5 dimensions"`
Replace with: `"FIRST: loss type and affected areas, then fundamental approach difference"`

**After all edits are complete on apps/api/src/prompts/writer_pass_v1.json:**
Copy the file verbatim to apps/worker/src/prompts/writer_pass_v1.json (the two files are always kept identical).
  </action>
  <verify>
  python -c "import json; d=json.load(open('apps/api/src/prompts/writer_pass_v1.json')); assert d['metadata']['version']=='2.2'; assert 'THREE sentences' in d['user']; assert 'APPROACH DIFFERENCE' in d['user']; assert 'approach_examples' in d['metadata']['style_requirements']['overview_structure']; print('api OK')"
  python -c "import json; d=json.load(open('apps/worker/src/prompts/writer_pass_v1.json')); assert d['metadata']['version']=='2.2'; print('worker OK')"
  diff apps/api/src/prompts/writer_pass_v1.json apps/worker/src/prompts/writer_pass_v1.json && echo "Files identical"
  </verify>
  <done>Both prompt files are at version 2.2. User prompt says THREE sentences. Critical rule says APPROACH DIFFERENCE. Metadata has approach_examples. Both files are identical.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep "DIFFERENCE DIMENSIONS" apps/api/src/prompts/writer_pass_v1.json` returns nothing
- [ ] `grep "APPROACH EXAMPLES" apps/api/src/prompts/writer_pass_v1.json` returns a match
- [ ] `grep "Do NOT default" apps/api/src/prompts/writer_pass_v1.json` returns nothing
- [ ] `grep '"version"' apps/api/src/prompts/writer_pass_v1.json` shows `"2.2"`
- [ ] `grep "THREE sentences" apps/api/src/prompts/writer_pass_v1.json` returns a match
- [ ] `diff apps/api/src/prompts/writer_pass_v1.json apps/worker/src/prompts/writer_pass_v1.json` shows no differences
- [ ] JSON is valid: `python -m json.tool apps/api/src/prompts/writer_pass_v1.json > /dev/null`
- [ ] Run writer pass tests: `cd apps/api && PYTHONPATH=/mnt/c/Users/benav.TUFFON_AMD/Documents/Projects/VIP30/packages/shared-python python -m pytest tests/test_writer_pass.py -v` — all pass
</verification>

<success_criteria>
- Both prompt files updated to v2.2
- APPROACH EXAMPLES table restored (7 pairs)
- DIFFERENCE DIMENSIONS section and "Do NOT default" instruction removed
- User prompt narrative placeholder corrected to THREE sentences
- Critical rule sentence 1 updated to reference APPROACH DIFFERENCE
- Metadata reflects approach-first structure
- Both files identical, valid JSON
- All writer pass tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-cost-driver-narrative-quality/20-01-SUMMARY.md`
</output>
