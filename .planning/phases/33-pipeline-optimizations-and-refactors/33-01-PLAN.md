---
phase: 33-parser-recap-trade-summary-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/parser/vip_parser/xactimate/parser.py
  - packages/parser/tests/conftest.py
  - packages/parser/tests/test_coverage.py
  - packages/parser/tests/test_recap_trade_summary_contract.py
autonomous: true
must_haves:
  truths:
    - "`recaps_and_summaries.recap_by_category` exists in parser output for every current corpus document and is emitted in canonical normalized structure"
    - "`recaps_and_summaries.trade_summary` exists as a fully parsed object when the source document contains Trade Summary and is `null` when the document truly lacks that section"
    - "State Farm trade summary rows preserve the columns and row data present in the source table"
    - "Parser contract tests explicitly fail when recap_by_category is present in source but missing in output"
    - "All parser tests added for recap/trade summary contract pass without relying on golden updates yet"
  artifacts:
    - path: "packages/parser/vip_parser/xactimate/parser.py"
      provides: "Stable parser output contract for recaps_and_summaries"
      contains: "recaps_and_summaries"
    - path: "packages/parser/tests/test_recap_trade_summary_contract.py"
      provides: "Targeted contract tests for recap_by_category and trade_summary presence/shape"
      contains: "test_trade_summary"
    - path: "packages/parser/tests/test_coverage.py"
      provides: "Coverage harness extended with hard-failure checks for recap/trade summary contract"
      contains: "test_section_coverage"
  key_links:
    - from: "parser.py:run"
      to: "parser.py:_parse_trade_summary_section"
      via: "recaps_and_summaries.trade_summary"
      pattern: "trade_summary contract"
    - from: "parser.py:run"
      to: "parser.py:_parse_recap_by_category_section"
      via: "recaps_and_summaries.recap_by_category"
      pattern: "recap_by_category contract"
---

<objective>
Harden the parser output contract for Phase 33. Ensure `recaps_and_summaries.recap_by_category` is always present in canonical form, and ensure `recaps_and_summaries.trade_summary` is fully parsed when present in the source or explicitly `null` when absent. Add deterministic parser tests that fail on missing recap/trade summary output before any golden refresh happens.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-pipeline-optimizations-and-refactors/33-CONTEXT.md
@.planning/phases/33-pipeline-optimizations-and-refactors/33-RESEARCH.md

@packages/parser/vip_parser/xactimate/parser.py
@packages/parser/tests/conftest.py
@packages/parser/tests/test_coverage.py

Key facts:
- `recaps_and_summaries` is already assembled in `parser.py:run()`
- `trade_summary` is currently conditionally included only when present; this plan makes absence explicit with `null`
- `recap_by_category` already has tolerant parsing logic and should be preserved as the canonical parser structure
- Current document corpus includes rough drafts, contractor-final, and State Farm final-draft examples
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing contract tests for recap_by_category and trade_summary</name>
  <files>packages/parser/tests/test_recap_trade_summary_contract.py, packages/parser/tests/test_coverage.py</files>
  <action>
Create a new parser test module that validates:
- every current corpus document emits `recaps_and_summaries.recap_by_category`
- documents with trade summary emit non-null `recaps_and_summaries.trade_summary`
- documents without trade summary emit `trade_summary is null`
- recap output has canonical structure (`subtotals` plus grouped category collections)

Extend `test_coverage.py` only as needed to make missing recap/trade summary a hard failure for the corpus.

Run the new tests RED first if necessary.
  </action>
  <verify>
    New tests clearly express Phase 33 contract expectations.
    Tests fail before implementation if the contract is not met.
  </verify>
  <done>
    Contract expectations for recap/trade summary are locked in tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update parser output contract</name>
  <files>packages/parser/vip_parser/xactimate/parser.py</files>
  <action>
Adjust parser payload assembly so that:
- `recaps_and_summaries.recap_by_category` is always populated from the best parsed source in canonical form
- `recaps_and_summaries.trade_summary` is always present as either a fully parsed object or `null`
- no duplicate top-level trade summary contract is introduced
- existing downstream payload fields remain compatible

Keep the recap structure canonical and stable for Xactimate category comparisons.
  </action>
  <verify>
    Parser output matches the new contract tests.
    Trade summary rows and totals are preserved when present.
  </verify>
  <done>
    Parser output contract is explicit and stable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run parser contract and regression tests</name>
  <files>(verification only)</files>
  <action>
Run targeted parser tests for the new contract plus existing parser coverage checks that do not require golden regeneration yet.
  </action>
  <verify>
    New contract tests pass.
    Existing parser tests still pass except for known pre-existing golden mismatches not yet addressed by later plans.
  </verify>
  <done>
    Phase 33 parser contract is verified and ready for wrapped-line work.
  </done>
</task>

</tasks>

<verification>
- [ ] `recaps_and_summaries.recap_by_category` always present in parser output for current corpus docs
- [ ] `recaps_and_summaries.trade_summary` is non-null only when the source contains a Trade Summary section
- [ ] canonical recap structure is validated in tests
- [ ] targeted parser tests pass
</verification>
