---
phase: 33-parser-recap-trade-summary-completeness
plan: 03
type: execute
wave: 3
depends_on: ["33-01", "33-02"]
files_modified:
  - packages/parser/tests/conftest.py
  - packages/parser/tests/test_coverage.py
  - packages/parser/tests/golden/final-drafts/bschacter.golden.json
  - packages/parser/tests/golden/final-drafts/statefarm/SF_BSchacter.golden.json
  - packages/parser/tests/golden/final-drafts/statefarm/lachman_sf.golden.json
  - packages/parser/tests/golden/final-drafts/statefarm/kalyvas_sf.golden.json
  - packages/parser/tests/golden/final-drafts/SCHACTER_RECON_6B_FINAL_DRAFT_CAR.golden.json
  - packages/parser/generate_gap_report.py
  - packages/parser/GAP-REPORT.md
autonomous: true
must_haves:
  truths:
    - "Affected golden JSON fixtures are regenerated from verified parser output after Plans 33-01 and 33-02"
    - "A new golden JSON file is created for `docs/final-drafts/SCHACTER_RECON_6B_FINAL_DRAFT_CAR.pdf` and wired into parser coverage"
    - "Full `trade_summary` objects are preserved in golden fixtures"
    - "Full parser golden suite passes after fixture updates"
    - "Gap report is regenerated after the parser/golden updates"
    - "Unrelated golden churn is reviewed case by case rather than accepted blindly"
  artifacts:
    - path: "packages/parser/tests/conftest.py"
      provides: "Expanded parser corpus including the new contractor-final document"
      contains: "DOCUMENTS"
    - path: "packages/parser/tests/golden/final-drafts/SCHACTER_RECON_6B_FINAL_DRAFT_CAR.golden.json"
      provides: "Golden baseline for the newly added final-draft source document"
      contains: "recaps_and_summaries"
    - path: "packages/parser/GAP-REPORT.md"
      provides: "Post-phase regenerated parser gap report"
      contains: "Coverage"
  key_links:
    - from: "packages/parser/tests/conftest.py:DOCUMENTS"
      to: "packages/parser/tests/golden"
      via: "document corpus and golden mapping"
      pattern: "golden coverage"
    - from: "packages/parser/tests/test_coverage.py"
      to: "packages/parser/GAP-REPORT.md"
      via: "same parser corpus, rerun after fixture updates"
      pattern: "regression reporting"
---

<objective>
Refresh the parser regression baseline after Phase 33 parser changes. Regenerate affected golden JSON files from verified parser output, add coverage for the newly added contractor-final PDF, rerun the full parser golden suite, and regenerate the gap report.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33-pipeline-optimizations-and-refactors/33-CONTEXT.md
@.planning/phases/33-pipeline-optimizations-and-refactors/33-RESEARCH.md
@.planning/phases/33-pipeline-optimizations-and-refactors/33-01-PLAN.md
@.planning/phases/33-pipeline-optimizations-and-refactors/33-02-PLAN.md

@packages/parser/tests/conftest.py
@packages/parser/tests/test_coverage.py
@packages/parser/tests/golden/README.md
@packages/parser/generate_gap_report.py

Key facts:
- existing parser corpus has 6 documents and corresponding goldens
- new file `docs/final-drafts/SCHACTER_RECON_6B_FINAL_DRAFT_CAR.pdf` requires a new golden and coverage entry
- goldens should reflect verified parser reality immediately after parser improvements
- full `trade_summary` must remain in fixture output
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend parser corpus and write failing coverage expectations for the new document</name>
  <files>packages/parser/tests/conftest.py, packages/parser/tests/test_coverage.py</files>
  <action>
Add the new contractor-final PDF to the parser test corpus and update any fixture mappings or harness assumptions needed so the coverage suite knows about it.

If necessary, add explicit coverage assertions for recap/trade-summary presence and the new golden path.
  </action>
  <verify>
    The harness references the new source file and golden destination.
    Coverage tests express the intended regression behavior for the expanded corpus.
  </verify>
  <done>
    Parser regression harness includes the full current document set.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate affected goldens from verified parser output</name>
  <files>packages/parser/tests/golden/final-drafts/*.json, packages/parser/tests/golden/final-drafts/statefarm/*.json</files>
  <action>
Generate the updated golden JSON files directly from parser output after Plans 33-01 and 33-02 are complete.

Include:
- affected existing final-draft goldens
- new golden for `SCHACTER_RECON_6B_FINAL_DRAFT_CAR.pdf`

Review any unrelated churn case by case before finalizing fixture updates.
  </action>
  <verify>
    Goldens are valid JSON.
    Full trade_summary content is preserved when present.
    New golden exists for the newly added PDF.
  </verify>
  <done>
    Golden baseline now matches verified parser behavior for the full corpus.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full parser regression suite and regenerate gap report</name>
  <files>packages/parser/GAP-REPORT.md</files>
  <action>
Run the full parser golden suite, targeted assertions from earlier plans, and regenerate the gap report after all fixture updates.
  </action>
  <verify>
    Full parser suite passes.
    Targeted recap/trade-summary and wrapped-line tests pass.
    Gap report is regenerated from the updated parser/golden baseline.
  </verify>
  <done>
    Phase 33 regression baseline is complete and revalidated.
  </done>
</task>

</tasks>

<verification>
- [ ] new contractor-final PDF is represented by a golden and coverage entry
- [ ] affected goldens are regenerated only after parser behavior is verified
- [ ] full parser suite passes
- [ ] gap report is regenerated
</verification>
