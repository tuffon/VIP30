---
phase: 33-parser-recap-trade-summary-completeness
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - packages/parser/vip_parser/xactimate/parser.py
  - packages/parser/tests/test_statefarm_wrapped_notes.py
autonomous: true
must_haves:
  truths:
    - "Known wrapped-description vs notes bug in `docs/final-drafts/statefarm/SF_BSchacter.pdf` line items `#2` and `#276` is addressed"
    - "Parser distinguishes wrapped description lines from true notes more accurately without introducing broad note-loss regressions"
    - "If ambiguity remains after heuristic/classifier evaluation, ambiguous text stays attached to notes"
    - "Targeted assertions exist for known wrapped-line outputs and known true note lines"
  artifacts:
    - path: "packages/parser/vip_parser/xactimate/parser.py"
      provides: "Improved wrapped-line vs notes detection in section parsing"
      contains: "_attach_pending_notes"
    - path: "packages/parser/tests/test_statefarm_wrapped_notes.py"
      provides: "Targeted regression tests for wrapped description and notes classification"
      contains: "test_sf_bschacter"
  key_links:
    - from: "parser.py:_parse_section"
      to: "parser.py:_attach_pending_notes"
      via: "pending_header_lines / collecting_notes flow"
      pattern: "wrapped description vs notes"
---

<objective>
Resolve the known State Farm wrapped-description vs notes bug for `SF_BSchacter.pdf`, especially line items `#2` and `#276`. Improve classification conservatively: detect strong wrapped-description cases, preserve true notes, and keep ambiguous text in notes if confidence is low.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33-pipeline-optimizations-and-refactors/33-CONTEXT.md
@.planning/phases/33-pipeline-optimizations-and-refactors/33-RESEARCH.md
@.planning/phases/33-pipeline-optimizations-and-refactors/33-01-PLAN.md

@packages/parser/vip_parser/xactimate/parser.py
@docs/final-drafts/statefarm/SF_BSchacter.pdf

Key facts:
- notes are currently accumulated via `pending_header_lines` and `_attach_pending_notes()`
- wrapped-description continuation likely needs to be intercepted before note attachment
- research into a lightweight local classifier is acceptable, but deterministic heuristics are preferred unless they prove insufficient
- final fallback for ambiguous text is to keep it in notes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing targeted tests for wrapped-line vs notes behavior</name>
  <files>packages/parser/tests/test_statefarm_wrapped_notes.py</files>
  <action>
Add focused parser tests for `SF_BSchacter.pdf` that assert:
- known wrapped-description examples (`#2`, `#276`) are classified correctly according to verified expected output
- at least one known true note case remains captured as notes
- ambiguous cases still bias toward notes when certainty is low

Keep these tests targeted and document-backed, not fuzzy high-level section checks.
  </action>
  <verify>
    Tests isolate the known bug and prevent future regressions.
    Tests fail before the parser heuristic is fixed.
  </verify>
  <done>
    Wrapped-line bug expectations are locked in tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement conservative wrapped-line detection</name>
  <files>packages/parser/vip_parser/xactimate/parser.py</files>
  <action>
Update section parsing so the parser can better detect when a following line is a wrapped continuation of the line-item description versus a true note line.

Allowed approaches:
- deterministic heuristics based on alignment/content patterns
- lightweight local classification aid if clearly justified

Do not aggressively steal ambiguous note text into descriptions. Preserve the conservative fallback to notes.
  </action>
  <verify>
    Known examples parse correctly.
    True notes are still captured.
    Ambiguous text stays in notes when certainty is low.
  </verify>
  <done>
    Wrapped-line vs notes behavior is improved and stable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run targeted parser bug tests</name>
  <files>(verification only)</files>
  <action>
Run the new targeted wrapped-line regression tests plus any nearby parser tests needed to confirm no immediate regression in section parsing.
  </action>
  <verify>
    Wrapped-line targeted tests pass.
    Parser section parsing remains stable for the known State Farm examples.
  </verify>
  <done>
    Wrapped-line bug fix is verified and ready for golden regeneration.
  </done>
</task>

</tasks>

<verification>
- [ ] `SF_BSchacter.pdf` known wrapped-line examples have explicit regression tests
- [ ] true notes are still captured where expected
- [ ] ambiguous text still defaults to notes
- [ ] targeted parser tests pass
</verification>
