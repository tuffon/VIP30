---
phase: 34-pipeline-improvements-using-the-recap-by-summary-and-trade-summary-output-from-the-json
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-03-11T19:17:07.503Z
---

<current_state>
Phase 34 discussion is paused before writing `34-CONTEXT.md`. The user selected all four discussion areas (`1,2,3,4`) and the next unresolved step is to answer the eight scoping questions about how the pipeline should use `recap_by_category` and `trade_summary`.
</current_state>

<completed_work>

- Phase 33 is fully executed and closed out in code and planning.
- Phase 34 was added to the roadmap and state, and its phase directory exists.
- Initial code review for Phase 34 was done:
  - `packages/shared-python/vip_shared/pipeline/passes/trade_context.py`
  - `packages/shared-python/vip_shared/pipeline/passes/cost_drivers.py`
  - `packages/shared-python/vip_shared/pipeline/cost_driver_pipeline.py`
  - `packages/shared-python/vip_shared/bid_comp/core.py`
  - `packages/shared-python/vip_shared/pipeline/passes/driver_pass.py`
  - `packages/shared-python/vip_shared/pipeline/models.py`
- Key architectural finding: parser JSON already exposes `recap_by_category` and `trade_summary`, but the current cost-driver/summary pipeline does not use that structured data deeply for ranking or grounding.
- Discussion framing was established for four areas:
  - data usage priority
  - visible output impact
  - fallback and conflict rules
  - granularity of improvement
- The user selected all four areas.
</completed_work>

<remaining_work>

- Get answers to the eight open Phase 34 scoping questions:
  - whether recap becomes the primary source for category totals/top deltas
  - whether trade summary influences selection/ranking or only evidence
  - how explicit recap/trade facts should appear in final output
  - whether output format stays overview/key-drivers or gains a trade-aware section
  - what source wins on recap vs recomputed-total conflicts
  - whether one-sided trade summary should still be used
  - whether the phase targets driver selection, final summary generation, or both
  - whether to reduce LLM dependence via more deterministic structured grounding
- Write `34-CONTEXT.md` in the phase directory once those answers are captured.
- After context is written, move to `$gsd-plan-phase 34`.
</remaining_work>

<decisions_made>

- Phase 34 scope is pipeline-side usage of existing parser JSON summary structures, not new parser extraction work.
- The relevant existing structured inputs are `recaps_and_summaries.recap_by_category` and `recaps_and_summaries.trade_summary`.
- Discussion should stay inside pipeline behavior and output quality, especially driver selection, evidence grounding, and summary generation.
- Use the Phase 34 directory for the handoff even though the latest `PLAN.md` on disk is from Phase 33, because the actively paused work is pre-planning discussion for Phase 34.
</decisions_made>

<blockers>
- No technical blocker.
- The only blocker is missing user decisions on the eight scoping questions, which are required before writing a clean `34-CONTEXT.md`.
</blockers>

<context>
The main mental model for Phase 34 is that Phase 33 made `recap_by_category` and `trade_summary` reliably present in parser output, but the narrative pipeline still mostly reasons from line items and category deltas alone. The likely direction is to use recap data for more deterministic category grounding and use trade summary as higher-level evidence, but the user has not yet decided how aggressive that should be. The exact open prompt already sent should be reused on resume rather than re-deriving it.
</context>

<next_action>
Start with the exact pending prompt already prepared for the user:

1. When `recap_by_category` exists, should it become the primary source for category totals and top-delta selection, replacing ad hoc derivation from line items unless it is missing?
2. When `trade_summary` exists, should it be used only as supporting evidence for narratives, or should it also influence which drivers are selected and ranked?
3. In the final narrative, do you want recap/trade-summary facts surfaced explicitly, such as category totals, percentages, and trade-level rollups, or mostly kept behind the scenes to improve accuracy?
4. Should the final output gain a distinct trade-summary-aware section or references, or should the existing overview/key-drivers format remain and simply become better grounded?
5. If `recap_by_category` disagrees with recomputed totals from parsed line items, which source should win by default: recap, recomputed line items, or whichever has stronger internal consistency?
6. If only one side has `trade_summary`, should the pipeline still use it asymmetrically, or only use trade summary when both documents have it?
7. Should this phase improve both cost-driver selection/ranking and final summary generation, or only one of those layers first?
8. If recap/trade data helps, should we reduce LLM dependence by passing more structured evidence into prompts and doing more deterministic ranking/grounding before the model is called?
</next_action>
