---
phase: 34-pipeline-improvements-using-the-recap-by-summary-and-trade-summary-output-from-the-json
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-python/vip_shared/pipeline/models.py
  - packages/shared-python/vip_shared/pipeline/passes/trade_context.py
  - packages/shared-python/vip_shared/pipeline/passes/cost_drivers.py
  - packages/shared-python/tests/test_trade_context.py
  - packages/shared-python/tests/test_cost_drivers.py
autonomous: true
must_haves:
  truths:
    - "`trade_summary` is treated as the preferred category evidence source when present because it already combines recap totals with associated line-item evidence"
    - "`recap_by_category` remains the fallback source when trade summary is absent or incomplete"
    - "Pipeline category evidence stays 1:1 with parsed category names and does not invent replacement taxonomy"
    - "Fallback line-item association work is limited to selected top-driver categories rather than all categories"
    - "Deterministic tests prove source preference and category evidence assembly behavior"
  artifacts:
    - path: "packages/shared-python/vip_shared/pipeline/models.py"
      provides: "Category-first pipeline evidence contracts"
      contains: "TradeContext"
    - path: "packages/shared-python/vip_shared/pipeline/passes/trade_context.py"
      provides: "Trade-summary-first category evidence builder"
      contains: "build_trade_context"
    - path: "packages/shared-python/vip_shared/pipeline/passes/cost_drivers.py"
      provides: "Top-driver-scoped fallback evidence mapping"
      contains: "map_driver_items"
    - path: "packages/shared-python/tests/test_trade_context.py"
      provides: "Deterministic source-preference and category evidence tests"
      contains: "trade_summary"
  key_links:
    - from: "trade_context.py:build_trade_context"
      to: "models.py:TradeContext"
      via: "category evidence source selection"
      pattern: "trade_summary first"
    - from: "cost_drivers.py:map_driver_items"
      to: "test_cost_drivers.py"
      via: "selected-category evidence gathering"
      pattern: "top-driver-scoped fallback"
---

<objective>
Create the category-evidence foundation for Phase 34. Elevate `trade_summary` from optional enrichment to the preferred source of category evidence when present, keep `recap_by_category` as the fallback, and ensure fallback line-item association work is focused on selected top-driver categories only.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-pipeline-improvements-using-the-recap-by-summary-and-trade-summary-output-from-the-json/34-CONTEXT.md
@.planning/phases/34-pipeline-improvements-using-the-recap-by-summary-and-trade-summary-output-from-the-json/34-RESEARCH.md

@packages/shared-python/vip_shared/pipeline/models.py
@packages/shared-python/vip_shared/pipeline/passes/trade_context.py
@packages/shared-python/vip_shared/pipeline/passes/cost_drivers.py
@packages/shared-python/tests/test_trade_context.py
@packages/shared-python/tests/test_cost_drivers.py

Key facts:
- current `TradeContext` keeps category totals and raw trade items separate
- current fallback line-item mapping works broadly from section `cat` codes but is not optimized for the selected-top-driver workflow
- Phase 34 needs the pipeline data model to reflect the rule: category totals govern first, line-item evidence supports those chosen categories second
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for trade-summary-first category evidence behavior</name>
  <files>packages/shared-python/tests/test_trade_context.py, packages/shared-python/tests/test_cost_drivers.py</files>
  <action>
Add deterministic tests that lock these expectations:
- when `trade_summary` exists, it is the preferred source for category evidence
- when `trade_summary` is absent, `recap_by_category` remains the fallback
- category names stay aligned to parsed recap/trade-summary names
- fallback evidence gathering can be scoped to selected top-driver categories instead of all categories

Keep the tests focused on data contracts and source preference rather than prompt behavior.
  </action>
  <verify>
    Tests fail before implementation if trade summary is not preferred or if fallback behavior stays too broad.
  </verify>
  <done>
    Phase 34 category-evidence expectations are locked in tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor TradeContext and evidence assembly around trade summary first</name>
  <files>packages/shared-python/vip_shared/pipeline/models.py, packages/shared-python/vip_shared/pipeline/passes/trade_context.py, packages/shared-python/vip_shared/pipeline/passes/cost_drivers.py</files>
  <action>
Update the pipeline data model and helpers so that:
- `trade_summary` becomes the preferred source when present
- `recap_by_category` remains the fallback source
- the resulting category model can carry source-aware evidence for downstream driver selection and prompts
- fallback item association work occurs only for the selected driver categories rather than all categories

Preserve the existing category naming contract and avoid inventing alternate taxonomy.
  </action>
  <verify>
    Category evidence contracts match the tests.
    Trade summary is used as the preferred source when available.
    Fallback evidence gathering remains correct for recap-only cases.
  </verify>
  <done>
    The pipeline has a category-first evidence foundation suitable for deterministic driver ranking.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run targeted shared-python tests for the new category evidence layer</name>
  <files>(verification only)</files>
  <action>
Run the updated trade-context and cost-driver tests to verify the new source-preference and fallback behavior before changing prompt or summary logic.
  </action>
  <verify>
    Targeted shared-python tests pass.
    The category evidence layer is stable enough for driver-pass changes.
  </verify>
  <done>
    Phase 34 foundation work is verified.
  </done>
</task>

</tasks>

<verification>
- [ ] `trade_summary` is the preferred category evidence source when present
- [ ] `recap_by_category` remains the fallback when trade summary is absent
- [ ] category names remain 1:1 with parsed names
- [ ] fallback evidence gathering is scoped to selected top-driver categories
- [ ] targeted shared-python tests pass
</verification>
