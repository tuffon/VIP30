---
phase: 13-package-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/parser/pyproject.toml
  - packages/parser/vip_parser/__init__.py
  - packages/parser/vip_parser/bni_extract_unit_costs.py
  - packages/parser/vip_parser/parse_dir.py
  - packages/parser/vip_parser/xactimate_rough_draft_parse.py
  - packages/parser/vip_parser/xactimate/__init__.py
  - packages/parser/vip_parser/xactimate/constants.py
  - packages/parser/vip_parser/xactimate/helpers.py
  - packages/parser/vip_parser/xactimate/io.py
  - packages/parser/vip_parser/xactimate/parser.py
  - packages/parser/vip_parser/xactimate/visible_text.py
  - apps/vip-parse/src/tasks.py
  - apps/vip-parse/src/worker_parse_helper.py
  - apps/vip-parse/src/api/render.py
  - apps/vip-parse/tests/test_xactimate_parser.py
  - apps/vip-parse/tests/test_visible_text.py
autonomous: true

must_haves:
  truths:
    - "packages/parser/ exists as a standalone Python package with pyproject.toml"
    - "Parser has zero dependencies on src/ business logic"
    - "src/preflight/ dead code is removed"
    - "All parser imports in src/ updated from 'from parse.xactimate' to 'from vip_parser.xactimate'"
  artifacts:
    - path: "packages/parser/pyproject.toml"
      provides: "Parser package metadata and dependencies"
      contains: "vip-parser"
    - path: "packages/parser/vip_parser/xactimate/parser.py"
      provides: "Core Xactimate PDF parser"
      min_lines: 2000
    - path: "packages/parser/vip_parser/__init__.py"
      provides: "Package init with public exports"
  key_links:
    - from: "apps/vip-parse/src/tasks.py"
      to: "packages/parser/vip_parser/xactimate"
      via: "from vip_parser.xactimate import XactimateRoughDraftParser"
      pattern: "from vip_parser"
---

<objective>
Extract the Xactimate PDF parser into a standalone package at packages/parser/ and remove preflight dead code.

Purpose: The parser is a self-contained library (3,861 lines) with zero business logic dependencies. Extracting it as a standalone package enforces the unidirectional dependency (parser → JSON → business logic) and makes it independently installable.

Output: packages/parser/ with pyproject.toml, vip_parser/ Python package, all parser imports updated, preflight dead code removed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Relevant source files:
@apps/vip-parse/parse/__init__.py
@apps/vip-parse/parse/xactimate/__init__.py
@apps/vip-parse/parse/xactimate/parser.py
@apps/vip-parse/src/tasks.py
@apps/vip-parse/src/worker_parse_helper.py
@apps/vip-parse/src/api/render.py
@apps/vip-parse/requirements-worker.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create packages/parser/ with pyproject.toml and move parser code</name>
  <files>
    packages/parser/pyproject.toml,
    packages/parser/vip_parser/__init__.py,
    packages/parser/vip_parser/bni_extract_unit_costs.py,
    packages/parser/vip_parser/parse_dir.py,
    packages/parser/vip_parser/xactimate_rough_draft_parse.py,
    packages/parser/vip_parser/xactimate/__init__.py,
    packages/parser/vip_parser/xactimate/constants.py,
    packages/parser/vip_parser/xactimate/helpers.py,
    packages/parser/vip_parser/xactimate/io.py,
    packages/parser/vip_parser/xactimate/parser.py,
    packages/parser/vip_parser/xactimate/visible_text.py
  </files>
  <action>
    1. Create `packages/parser/` directory structure:
       ```
       packages/parser/
         pyproject.toml
         vip_parser/
           __init__.py
           bni_extract_unit_costs.py
           parse_dir.py
           xactimate_rough_draft_parse.py
           xactimate/
             __init__.py
             constants.py
             helpers.py
             io.py
             parser.py
             visible_text.py
       ```

    2. Create `packages/parser/pyproject.toml`:
       ```toml
       [build-system]
       requires = ["setuptools>=70.0.0"]
       build-backend = "setuptools.build_meta"

       [project]
       name = "vip-parser"
       version = "0.1.0"
       description = "Xactimate PDF estimate parser"
       requires-python = ">=3.11"
       dependencies = [
           "pdfplumber>=0.10.0",
           "pypdfium2>=4.30.0",
       ]

       [tool.setuptools.packages.find]
       include = ["vip_parser*"]
       ```

    3. Move files from `apps/vip-parse/parse/` to `packages/parser/vip_parser/`:
       - `parse/__init__.py` → `vip_parser/__init__.py`
       - `parse/bni_extract_unit_costs.py` → `vip_parser/bni_extract_unit_costs.py`
       - `parse/parse_dir.py` → `vip_parser/parse_dir.py`
       - `parse/xactimate_rough_draft_parse.py` → `vip_parser/xactimate_rough_draft_parse.py`
       - `parse/xactimate/*.py` → `vip_parser/xactimate/*.py`

    4. Update internal imports within the moved files:
       - In `vip_parser/xactimate_rough_draft_parse.py`: change `from parse.xactimate` to `from vip_parser.xactimate`
       - In `vip_parser/parse_dir.py`: change `from parse.xactimate` to `from vip_parser.xactimate`
       - In `vip_parser/__init__.py`: update docstring, keep minimal

    5. Update `vip_parser/__init__.py` to expose public API:
       ```python
       """Xactimate PDF estimate parser package."""
       from vip_parser.xactimate import XactimateRoughDraftParser

       __all__ = ["XactimateRoughDraftParser"]
       ```

    6. Delete the original `apps/vip-parse/parse/` directory (use git mv where possible for history).

    7. Add editable install to requirements files. In `apps/vip-parse/requirements-worker.txt`, add at the END:
       ```
       -e ../../packages/parser
       ```
       In `apps/vip-parse/requirements-web.txt`, add at the END (render.py uses the parser):
       ```
       -e ../../packages/parser
       ```
       In `apps/vip-parse/requirements.txt`, add at the END:
       ```
       -e ../../packages/parser
       ```

    8. Update ALL parser imports in apps/vip-parse/src/ and tests/:
       - `src/tasks.py` lines 173, 207: `from parse.xactimate import XactimateRoughDraftParser` → `from vip_parser.xactimate import XactimateRoughDraftParser`
       - `src/worker_parse_helper.py` line 25: same change
       - `src/api/render.py` line 19: same change
       - `tests/test_xactimate_parser.py` lines 9-10: `from parse.xactimate.helpers` → `from vip_parser.xactimate.helpers`, `from parse.xactimate.parser` → `from vip_parser.xactimate.parser`
       - `tests/test_visible_text.py` line 6: `from parse.xactimate.visible_text` → `from vip_parser.xactimate.visible_text`
  </action>
  <verify>
    - `ls packages/parser/pyproject.toml` exists
    - `ls packages/parser/vip_parser/xactimate/parser.py` exists
    - `! test -d apps/vip-parse/parse` (original deleted)
    - `grep -r "from parse\." apps/vip-parse/src/ apps/vip-parse/tests/` returns NO matches (all updated)
    - `grep -r "from vip_parser" apps/vip-parse/src/ apps/vip-parse/tests/` returns matches confirming updates
  </verify>
  <done>Parser package exists at packages/parser/ with pyproject.toml, all imports updated from `parse.xactimate` to `vip_parser.xactimate`, original parse/ directory removed</done>
</task>

<task type="auto">
  <name>Task 2: Remove preflight dead code</name>
  <files>
    apps/vip-parse/src/preflight/__init__.py,
    apps/vip-parse/src/preflight/analysis.py,
    apps/vip-parse/src/preflight/cli.py,
    apps/vip-parse/src/preflight/ocr.py,
    apps/vip-parse/src/preflight/pdf_preflight.py,
    apps/vip-parse/tests/test_pdf_preflight.py
  </files>
  <action>
    1. Delete the entire `apps/vip-parse/src/preflight/` directory (5 files, 549 lines).
       This module is not imported anywhere in the main application — only by its own test file and standalone CLI.

    2. Delete `apps/vip-parse/tests/test_pdf_preflight.py` — tests for removed module.

    3. Verify no remaining references: grep for `preflight`, `PDFPreflight`, `PreflightReport` in src/ and tests/.
       The only acceptable match would be in git history, not in current code.
  </action>
  <verify>
    - `! test -d apps/vip-parse/src/preflight` (directory removed)
    - `! test -f apps/vip-parse/tests/test_pdf_preflight.py` (test removed)
    - `grep -r "preflight\|PDFPreflight\|PreflightReport" apps/vip-parse/src/ apps/vip-parse/tests/` returns NO matches
  </verify>
  <done>Preflight dead code removed — 5 source files and 1 test file deleted, zero remaining references</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `packages/parser/pyproject.toml` exists with correct metadata
- [ ] `packages/parser/vip_parser/xactimate/parser.py` exists (core parser file)
- [ ] No `from parse.xactimate` imports remain in apps/vip-parse/
- [ ] All parser imports use `from vip_parser.xactimate`
- [ ] `apps/vip-parse/parse/` directory is deleted
- [ ] `apps/vip-parse/src/preflight/` directory is deleted
- [ ] `apps/vip-parse/tests/test_pdf_preflight.py` is deleted
- [ ] No references to `preflight` remain in source or test files
</verification>

<success_criteria>

- Parser package at packages/parser/ with pyproject.toml and all parser code
- Parser has zero dependencies on src/ business logic (only pdfplumber, pypdfium2)
- All 5 parser import sites updated to use vip_parser
- Preflight dead code (6 files) removed
- Requirements files reference parser package via editable install
</success_criteria>

<output>
After completion, create `.planning/phases/13-package-extraction/13-01-SUMMARY.md`
</output>
