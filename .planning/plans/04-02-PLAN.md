---
phase: 04-frontend-usage-polish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/vipclaims-saas/app/bid-comp/page.tsx
  - apps/vipclaims-saas/app/jobs/page.tsx
  - apps/vipclaims-saas/components/JobProgress.tsx
  - apps/vipclaims-saas/components/JobHistoryList.tsx
autonomous: false

must_haves:
  truths:
    - "User sees real-time job progress (state + percent) during processing"
    - "User can view job history list at /jobs"
    - "Job history shows status, date, credit cost for each job"
    - "Job history supports pagination (load more or page numbers)"
    - "Empty state shows 'No jobs yet' when history is empty"
    - "Failed jobs show error message and retry button"
    - "User cannot start job if credits are 0 (shows insufficient credits error)"
  artifacts:
    - path: "apps/vipclaims-saas/app/jobs/page.tsx"
      provides: "Job history page with pagination"
      min_lines: 60
    - path: "apps/vipclaims-saas/components/JobProgress.tsx"
      provides: "Real-time job progress component"
      min_lines: 40
    - path: "apps/vipclaims-saas/components/JobHistoryList.tsx"
      provides: "Paginated job list component"
      min_lines: 50
  key_links:
    - from: "bid-comp page job submission"
      to: "POST /jobs"
      via: "authenticated job creation"
      pattern: "fetch.*\\/jobs.*POST"
    - from: "JobProgress polling"
      to: "GET /jobs/{id}"
      via: "polling interval"
      pattern: "fetch.*\\/jobs\\/.*GET"
    - from: "JobHistoryList"
      to: "GET /jobs?page="
      via: "paginated list fetch"
      pattern: "fetch.*\\/jobs\\?page"
---

<objective>
Implement real-time job progress and job history with pagination.

Purpose: Show users their job status in real-time and provide history view.
Output: Working job progress display and paginated job history page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/plans/04-01-SUMMARY.md

@apps/vipclaims-saas/app/bid-comp/page.tsx
@apps/vip-parse/src/routes/jobs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JobProgress component with real-time polling</name>
  <files>apps/vipclaims-saas/components/JobProgress.tsx</files>
  <action>
Create apps/vipclaims-saas/components/JobProgress.tsx with:

1. **Props:**
   - jobId: string
   - onComplete?: (result) => void
   - onError?: (error) => void

2. **State tracking:**
   - state: "queued" | "parsing" | "analyzing" | "writing" | "completed" | "failed"
   - progress_percent: number (0-100)
   - current_step: string | null
   - error_message: string | null

3. **Polling logic:**
   - Poll GET `${API_BASE}/jobs/${jobId}` every 2 seconds
   - Use credentials: "include" for auth
   - Update state on each response
   - Stop polling when state is "completed" or "failed"
   - Call onComplete/onError callbacks

4. **Visual progress:**
   - Progress bar showing percent
   - State badge (Queued, Parsing, Analyzing, Writing, Completed, Failed)
   - Current step text if available
   - Estimated time remaining (optional, based on progress)

5. **State colors:**
   - queued: slate/gray
   - parsing/analyzing/writing: amber/yellow (in progress)
   - completed: emerald/green
   - failed: rose/red

6. **Error display:**
   - If failed, show error_message
   - Show retry button (calls parent's retry handler)

Use useEffect with setInterval for polling. Clean up on unmount.
  </action>
  <verify>pnpm --filter vipclaims-saas build succeeds</verify>
  <done>JobProgress component polls job status and displays real-time progress.</done>
</task>

<task type="auto">
  <name>Task 2: Update bid-comp page to use authenticated jobs API</name>
  <files>apps/vipclaims-saas/app/bid-comp/page.tsx</files>
  <action>
Update apps/vipclaims-saas/app/bid-comp/page.tsx:

1. **Change API endpoint:**
   - Instead of POST /render/bid-comp/keys, use POST /jobs
   - Include credentials: "include" for cookie auth
   - Same payload: carrier_key, contractor_key, carrier_filename, contractor_filename

2. **Handle insufficient credits (402):**
   - If response.status === 402, show error: "Insufficient credits. Please add credits to continue."
   - Don't proceed with polling

3. **Handle unauthenticated (401):**
   - Redirect to /login with return URL

4. **Use JobProgress component:**
   - After job creation, render JobProgress with the job_id
   - Replace existing inline polling with component

5. **Show credit balance before submission:**
   - Display current credits near submit button
   - Disable submit if credits === 0

6. **Maintain backward compatibility:**
   - Keep existing upload logic (presigned URLs)
   - Only change the job creation endpoint
  </action>
  <verify>pnpm --filter vipclaims-saas build succeeds</verify>
  <done>bid-comp page uses /jobs API, shows credit check, uses JobProgress component.</done>
</task>

<task type="auto">
  <name>Task 3: Create job history page with pagination</name>
  <files>apps/vipclaims-saas/app/jobs/page.tsx, apps/vipclaims-saas/components/JobHistoryList.tsx</files>
  <action>
Create apps/vipclaims-saas/components/JobHistoryList.tsx:

1. **Props:**
   - jobs: array of job objects
   - totalCount: number
   - page: number
   - perPage: number
   - onPageChange: (page) => void
   - onRetry: (jobId) => void

2. **Job row display:**
   - Status badge (color-coded)
   - Created date (formatted nicely)
   - Primary filename vs Comparison filename (truncated)
   - Credit cost (always 1 for now)
   - Download button if completed
   - Retry button if failed

3. **Empty state:**
   - "No jobs yet" message with nice illustration/icon
   - Link to /bid-comp: "Start your first comparison"

4. **Pagination:**
   - Page numbers or "Load more" button
   - Show "Page X of Y" or "Showing X-Y of Z"

Create apps/vipclaims-saas/app/jobs/page.tsx:

1. **Data fetching:**
   - GET `${API_BASE}/jobs?page=1&per_page=20`
   - Use credentials: "include"
   - Handle loading/error states

2. **Authentication check:**
   - If 401, redirect to /login

3. **Page title:**
   - "Job History" heading

4. **Render JobHistoryList:**
   - Pass fetched jobs and pagination props

5. **Retry handler:**
   - POST /jobs/{id}/retry
   - On success, refresh list

Use "use client" directive. Consider using SWR or React Query for cleaner data fetching.
  </action>
  <verify>pnpm --filter vipclaims-saas build succeeds</verify>
  <done>Job history page at /jobs shows paginated list with status, dates, and actions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Job progress and history UI</what-built>
  <how-to-verify>
    1. Start backend and frontend (see 04-01 verify steps)
    2. Log in via /login flow
    3. Go to /bid-comp
    4. Verify credit balance is shown
    5. Upload two PDFs and submit
    6. Watch real-time progress (should show states: parsing → analyzing → writing → completed)
    7. Verify progress percentage updates
    8. After completion, go to /jobs
    9. Verify job appears in history with correct status
    10. Click download link on completed job
    11. Test with 0 credits: should show "insufficient credits" error
    12. Test /jobs with no jobs: should show empty state
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm --filter vipclaims-saas build succeeds
- [ ] Job progress shows real-time updates
- [ ] Job history page loads and displays jobs
- [ ] Pagination works
- [ ] Empty state displays correctly
- [ ] Insufficient credits error displays
</verification>

<success_criteria>

- Real-time job progress visible during processing
- Job history page shows all user's jobs
- Pagination works for job list
- Clear empty/error states
- Human verified the flow
</success_criteria>

<output>
After completion, create `.planning/plans/04-02-SUMMARY.md`
</output>
