---
phase: 04-frontend-usage-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/vipclaims-saas/app/login/page.tsx
  - apps/vipclaims-saas/app/login/verify/page.tsx
  - apps/vipclaims-saas/components/CreditBalance.tsx
  - apps/vipclaims-saas/components/AuthLayout.tsx
  - apps/vipclaims-saas/app/layout.tsx
autonomous: false

must_haves:
  truths:
    - "User can enter email on /login and submit to request OTP"
    - "User sees loading state during OTP request"
    - "User sees error message for rate limit or invalid email"
    - "User is redirected to /login/verify after successful OTP request"
    - "User can enter 6-digit code on /login/verify"
    - "User sees specific error messages (expired, invalid, too_many_attempts)"
    - "User is redirected to /bid-comp after successful verification"
    - "Credit balance is displayed in header/navbar after login"
  artifacts:
    - path: "apps/vipclaims-saas/app/login/page.tsx"
      provides: "Email entry form with validation and OTP request"
      min_lines: 50
    - path: "apps/vipclaims-saas/app/login/verify/page.tsx"
      provides: "OTP verification form with code entry"
      min_lines: 50
    - path: "apps/vipclaims-saas/components/CreditBalance.tsx"
      provides: "Credit balance display component"
      min_lines: 20
  key_links:
    - from: "login/page.tsx form submit"
      to: "POST /auth/otp/send"
      via: "fetch call to backend API"
      pattern: "fetch.*auth/otp/send"
    - from: "login/verify/page.tsx form submit"
      to: "POST /auth/otp/verify"
      via: "fetch call with credentials"
      pattern: "fetch.*auth/otp/verify"
    - from: "CreditBalance component"
      to: "GET /auth/me or GET /credits/balance"
      via: "API call for balance"
---

<objective>
Implement auth UI (login + OTP verification) and credit balance display.

Purpose: Provide the user-facing authentication flow and credit visibility.
Output: Working login flow with email entry, OTP verification, and credit display in header.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/vipclaims-saas/app/layout.tsx
@apps/vipclaims-saas/app/bid-comp/page.tsx
@apps/vipclaims-saas/components/NavAuth.tsx
@apps/vip-parse/src/routes/auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create login page with email entry</name>
  <files>apps/vipclaims-saas/app/login/page.tsx</files>
  <action>
Create apps/vipclaims-saas/app/login/page.tsx with:

1. **Email entry form:**
   - Input field for email with validation
   - Submit button "Send verification code"
   - Loading state during submission

2. **API integration:**
   - On submit, POST to `${API_BASE}/auth/otp/send` with `{ email }`
   - Use credentials: "include" for cookie handling

3. **Error handling:**
   - Rate limit (429): "Too many requests. Please wait before trying again."
   - Invalid email (400): "Please enter a valid email address."
   - Generic error: "Something went wrong. Please try again."

4. **Success flow:**
   - On 200 response, redirect to `/login/verify?email={email}` (URL-encoded)
   - Store email in URL query param for verify page

5. **Styling:**
   - Use existing Tailwind classes from bid-comp page as reference
   - Center the form with max-width-sm
   - Clean, minimal design matching existing brand

Get API_BASE from process.env.NEXT_PUBLIC_API_BASE_URL.
Use "use client" directive for client-side interactivity.
  </action>
  <verify>pnpm --filter vipclaims-saas build succeeds without errors</verify>
  <done>Login page exists at /login with email form that calls POST /auth/otp/send and redirects to verify.</done>
</task>

<task type="auto">
  <name>Task 2: Create OTP verification page</name>
  <files>apps/vipclaims-saas/app/login/verify/page.tsx</files>
  <action>
Create apps/vipclaims-saas/app/login/verify/page.tsx with:

1. **Code entry form:**
   - 6-digit code input (can be single input or 6 separate inputs)
   - Display email from query param (read-only)
   - "Verify code" button
   - "Resend code" link that posts to /auth/otp/send again

2. **API integration:**
   - On submit, POST to `${API_BASE}/auth/otp/verify` with `{ email, code }`
   - Use credentials: "include" for cookie handling (JWT cookie will be set)

3. **Error handling with specific messages:**
   - error="invalid": "Invalid code. Please check and try again."
   - error="expired": "Code expired. Please request a new one."
   - error="too_many_attempts": "Too many attempts. Please request a new code."
   - Generic: "Verification failed. Please try again."

4. **Success flow:**
   - On 200 response, router.push("/bid-comp")
   - The JWT cookie is automatically set by the backend

5. **Back link:**
   - "Use different email" link that goes back to /login

6. **Styling:**
   - Match login page styling
   - Clear visual hierarchy
   - Code input should be prominent

Use useSearchParams() hook to read email from URL.
Use "use client" directive.
  </action>
  <verify>pnpm --filter vipclaims-saas build succeeds without errors</verify>
  <done>Verify page exists at /login/verify with OTP form that calls POST /auth/otp/verify and redirects to /bid-comp.</done>
</task>

<task type="auto">
  <name>Task 3: Create CreditBalance component</name>
  <files>apps/vipclaims-saas/components/CreditBalance.tsx, apps/vipclaims-saas/app/layout.tsx</files>
  <action>
Create apps/vipclaims-saas/components/CreditBalance.tsx with:

1. **Balance display:**
   - Fetch balance from `${API_BASE}/auth/me` (which returns user + workspace with balance)
   - Or create dedicated `/credits/balance` endpoint if /auth/me doesn't include balance
   - Display: "5 credits" or similar
   - Use credentials: "include" for cookie auth

2. **Loading state:**
   - Show "..." or skeleton while loading

3. **Unauthenticated state:**
   - If 401, don't display anything (user not logged in)

4. **Styling:**
   - Small, fits in navbar
   - Use emerald color for positive balance, amber for low (1-2), rose for 0

5. **Auto-refresh:**
   - Optionally use SWR or React Query for auto-refresh
   - Or simple useEffect with fetch on mount

Update apps/vipclaims-saas/app/layout.tsx:
- Import CreditBalance component
- Add to header/navbar area (near NavAuth or in a header bar)
- Only show when user is authenticated (wrap in conditional or let component handle it)

Note: The existing layout uses next-auth SessionProvider. The new auth uses HTTP-only cookies without next-auth. Need to handle this:
- Option A: Remove next-auth usage and use our cookie-based auth
- Option B: Keep next-auth for now but add CreditBalance that checks our API directly

For MVP, go with Option A (simpler, consistent auth model).
  </action>
  <verify>pnpm --filter vipclaims-saas build succeeds without errors</verify>
  <done>CreditBalance component exists and displays balance in layout. Authenticated users see their credit count.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Auth UI flow with login, OTP verification, and credit display</what-built>
  <how-to-verify>
    1. Start backend: cd apps/vip-parse && uvicorn src.api.main:app --reload --port 4000
    2. Start frontend: pnpm --filter vipclaims-saas dev
    3. Visit http://localhost:3000/login
    4. Enter a test email and submit
    5. Check backend logs for OTP code (or check email if Resend configured)
    6. Navigate to /login/verify?email=your@email.com
    7. Enter the 6-digit code
    8. Verify redirect to /bid-comp
    9. Verify credit balance displays in header
    10. Test error states: wrong code, expired code (wait 10 min), rate limit
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm --filter vipclaims-saas build succeeds
- [ ] /login page renders and submits correctly
- [ ] /login/verify page renders and verifies correctly
- [ ] CreditBalance displays in header after login
- [ ] Error states display clearly
</verification>

<success_criteria>

- Login flow works end-to-end (email → OTP → logged in)
- Credit balance visible after authentication
- Error messages are clear and specific
- UI matches existing design patterns
- Human verified the flow
</success_criteria>

<output>
After completion, create `.planning/plans/04-01-SUMMARY.md`
</output>
