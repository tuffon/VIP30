---
phase: 04-frontend-usage-polish
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - apps/vipclaims-saas/app/credits/page.tsx
  - apps/vipclaims-saas/components/CreditHistoryList.tsx
  - apps/vip-parse/src/routes/credits.py
  - apps/vip-parse/src/api/main.py
  - apps/vipclaims-saas/app/bid-comp/page.tsx
  - apps/vipclaims-saas/app/layout.tsx
autonomous: false

must_haves:
  truths:
    - "User can view credit transaction history at /credits"
    - "Credit history shows grants and consumptions with dates"
    - "Credit history supports pagination"
    - "UI copy reflects 'bid comparison' terminology"
    - "Internal naming uses comparison_job, bid_input"
    - "Empty state shows 'No transactions yet'"
  artifacts:
    - path: "apps/vipclaims-saas/app/credits/page.tsx"
      provides: "Credit transaction history page"
      min_lines: 50
    - path: "apps/vipclaims-saas/components/CreditHistoryList.tsx"
      provides: "Paginated credit transaction list"
      min_lines: 40
    - path: "apps/vip-parse/src/routes/credits.py"
      provides: "GET /credits endpoint for transaction history"
  key_links:
    - from: "CreditHistoryList"
      to: "GET /credits"
      via: "paginated history fetch"
      pattern: "fetch.*\\/credits"
---

<objective>
Implement credit transaction history and complete rebrand to "bid comparison" terminology.

Purpose: Provide credit visibility and consistent branding.
Output: Credit history page and updated terminology throughout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/plans/04-01-SUMMARY.md
@.planning/plans/04-02-SUMMARY.md

@apps/vip-parse/src/services/credits.py
@apps/vip-parse/src/db/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create credit history API endpoint</name>
  <files>apps/vip-parse/src/routes/credits.py, apps/vip-parse/src/api/main.py</files>
  <action>
Create apps/vip-parse/src/routes/credits.py:

1. **Router setup:**
   - `credits_router = APIRouter(prefix="/credits", tags=["credits"])`

2. **GET /credits - List credit transactions:**
   - Depends on: require_auth
   - Query params: page (default 1), per_page (default 20, max 100)
   - Fetch both grants and consumptions for workspace
   - Combine into single list with type indicator
   - Sort by created_at DESC
   - Return: { items: [...], total_count, page, per_page }

3. **Transaction item shape:**
   ```json
   {
     "id": "uuid",
     "type": "grant" | "consumption",
     "amount": 5,
     "source": "signup_bonus" | "job_completion",  // for grants
     "job_id": null | "uuid",  // for consumptions
     "created_at": "2026-02-14T10:00:00Z",
     "notes": "Trial credits for new signup"  // optional
   }
   ```

4. **GET /credits/balance - Just the balance:**
   - Depends on: require_auth
   - Returns: { balance: 5 }
   - This is a simpler endpoint for CreditBalance component

Update apps/vip-parse/src/api/main.py:
- Import credits_router from src.routes.credits
- Add: app.include_router(credits_router)
  </action>
  <verify>python -c "from src.routes.credits import credits_router; print('credits_router OK')" (from apps/vip-parse/)</verify>
  <done>Credit history endpoint exists at GET /credits with pagination. Balance endpoint at GET /credits/balance.</done>
</task>

<task type="auto">
  <name>Task 2: Create credit history page</name>
  <files>apps/vipclaims-saas/app/credits/page.tsx, apps/vipclaims-saas/components/CreditHistoryList.tsx</files>
  <action>
Create apps/vipclaims-saas/components/CreditHistoryList.tsx:

1. **Props:**
   - transactions: array of transaction objects
   - totalCount: number
   - page: number
   - perPage: number
   - onPageChange: (page) => void

2. **Transaction row display:**
   - Type badge: "Grant" (emerald) or "Used" (amber)
   - Amount: "+5" or "-1"
   - Source/reason: "Trial credits" or "Job #abc123"
   - Date: formatted nicely

3. **Empty state:**
   - "No transactions yet" message
   - Subtext: "Your credit activity will appear here"

4. **Pagination:**
   - Same pattern as JobHistoryList

Create apps/vipclaims-saas/app/credits/page.tsx:

1. **Data fetching:**
   - GET `${API_BASE}/credits?page=1&per_page=20`
   - Use credentials: "include"
   - Handle loading/error states

2. **Authentication check:**
   - If 401, redirect to /login

3. **Page header:**
   - "Credit History" heading
   - Current balance displayed prominently

4. **Render CreditHistoryList:**
   - Pass fetched transactions and pagination
  </action>
  <verify>pnpm --filter vipclaims-saas build succeeds</verify>
  <done>Credit history page at /credits shows paginated transactions with grants and consumptions.</done>
</task>

<task type="auto">
  <name>Task 3: Rebrand UI copy to "bid comparison" terminology</name>
  <files>apps/vipclaims-saas/app/bid-comp/page.tsx, apps/vipclaims-saas/app/layout.tsx, apps/vipclaims-saas/components/NavAuth.tsx</files>
  <action>
Update terminology across frontend:

1. **apps/vipclaims-saas/app/bid-comp/page.tsx:**
   - Change "ScopeVista" references to product name or remove
   - Ensure "bid comparison" or "Bid Comp" terminology
   - Update any "vip_job" references in comments to "comparison_job"

2. **apps/vipclaims-saas/app/layout.tsx:**
   - Update any "VIPClaims" branding to final product name
   - Ensure navigation labels are clear ("Bid Comp", "Job History", "Credits")

3. **Navigation links:**
   - Add nav link to /jobs (Job History)
   - Add nav link to /credits (Credits)
   - Ensure links only show when authenticated

4. **apps/vipclaims-saas/components/NavAuth.tsx:**
   - Update logout button text if needed
   - Ensure auth state reflects cookie-based auth (not next-auth)

5. **Button labels:**
   - "Generate Bid Comp" → keep or "Compare Estimates"
   - "Download XLSX" → keep

6. **Error messages:**
   - Ensure all error messages use "job" or "comparison" consistently
   - No references to internal names like "vip_job"

Note: NAME-01 (vip_job → comparison_job) and NAME-02 (raw_upload → bid_input) are internal backend naming.
Those would be done in backend code, not frontend. For now, focus on user-facing strings (NAME-03).
  </action>
  <verify>pnpm --filter vipclaims-saas build succeeds</verify>
  <done>UI copy consistently uses "bid comparison" terminology. Navigation includes all main sections.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Credit history and rebranded UI</what-built>
  <how-to-verify>
    1. Start backend and frontend
    2. Log in
    3. Go to /credits
    4. Verify credit history shows (at minimum, signup grant)
    5. Run a job to completion
    6. Verify consumption appears in credit history
    7. Check navigation: should have links to Bid Comp, Job History, Credits
    8. Verify all UI copy uses "bid comparison" terminology
    9. No references to "ScopeVista", "VIPClaims", or internal names
    10. Empty states display correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pnpm --filter vipclaims-saas build succeeds
- [ ] GET /credits endpoint works
- [ ] Credit history page displays transactions
- [ ] UI copy is consistent with "bid comparison" terminology
- [ ] Navigation includes all sections
</verification>

<success_criteria>

- Credit transaction history visible with pagination
- UI consistently uses "bid comparison" terminology
- Navigation complete with all main sections
- Human verified the rebrand
</success_criteria>

<output>
After completion, create `.planning/plans/04-03-SUMMARY.md`
</output>
