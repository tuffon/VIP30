"""src – top-level Python package for VIP30.

The file is intentionally *empty*; its mere presence allows `importlib` to
locate sub-modules such as `src.workers.echo` which are dynamically imported
by the orchestration layer.
""" 